import { registerRootComponent } from 'expo';
import App from './App';

// Register the main App component, so Expo can handle loading, errors, and ensure it works across all platforms.
registerRootComponent(App);